.text-center
  %h3.general-login-title= t('log_in')
  = link_to user_facebook_omniauth_authorize_path, class: "general-login-icon" do
    %i.fa.fa-facebook-official
  %p.general-login-text= t('or')
= render partial: 'sign_up_form', locals: { user: @user }
%p.general-sign-text.text-center
  = t('have_account')
  = link_to t('sign_in'), new_user_session_path, class: 'in-gold-500'
= render partial: 'google_map'
